{
  "name": "fengtian-f4club",
  "type": "module",
  "version": "1.0.0",
  "description": "奉天F4Club - Private community for four former game dev colleagues",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "db:init": "wrangler d1 execute f4club-db --local --file=db/schema.sql -y",
    "db:init:remote": "wrangler d1 execute f4club-db --remote --file=db/schema.sql -y",
    "db:verify:remote": "wrangler d1 execute f4club-db --remote --command=\"SELECT id, nickname, role, avatar_emoji, password_hash != '' AS has_password FROM users ORDER BY id\" -y"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "astro": "^5.0.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.85.0"
  }
}
